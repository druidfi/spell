#
# Enviromental values for Makefile and for docker-compose.yml
#

# Docker Compose project name
COMPOSE_PROJECT_NAME=mysite

# Local hostname
DRUPAL_HOSTNAME=mysite.docker.sh

# Docker image
DRUPAL_IMAGE=druidfi/drupal:7.3-web

# Default source @alias where to sync database and files from
DRUPAL_SYNC_SOURCE=master

# Public webroot
DRUPAL_WEBROOT=public

# APP_ENV dev|prod
APP_ENV=dev

# Docker CLI container
CLI_SERVICE=app
CLI_USER=druid
CLI_SHELL=bash
